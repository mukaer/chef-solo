<%= @log %> {
        daily
        missingok
        rotate 52
        compress
        delaycompress
        notifempty
	create 640 <%= @user %> <%= @group %>
        sharedscripts
	postrotate

	[ -f <%= @pid %> ] && kill -USR1 `cat <%= @pid %>`
	endscript
}